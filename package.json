{
  "name": "notes-crud-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --dist-dir dist"
  },
  "devDependencies": {
    "parcel": "^2.16.3"
  }
}
